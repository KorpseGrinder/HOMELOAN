spring:
  datasource:
    url: jdbc:oracle:thin:@localhost:1521/XEPDB1
    username: hlms_app
    password: hlms_password
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    show-sql: true
logging:
  level:
    com.hlms: DEBUG
    org.hibernate.SQL: DEBUG
